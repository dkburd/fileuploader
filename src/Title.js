import React from 'react';

// const Title = ({ children }) => (
//   <h1>{children}</h1>
// );

// export default Title;

export default function TitleTwo() {
  return (
    <div>
      <h1>title here</h1>
    </div>
  )
}